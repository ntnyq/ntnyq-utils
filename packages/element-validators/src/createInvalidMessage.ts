/**
 * @file createInvalidMessage
 */

/**
 * create invalid message
 * @param field - field name
 * @returns message
 */
export function createInvalidMessage(field: string) {
  return `${field}格式不正确，请重新输入`
}
