/**
 * @file defineFormRules
 */

import type { FormRules } from './types'

/**
 * `defineFormRules` helpers
 * @param rules - form rules
 * @returns form rules
 */
export function defineFormRules(rules: FormRules) {
  return rules
}
